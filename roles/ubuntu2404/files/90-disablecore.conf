# Ensure core dumps are restricted. Deliberately unnumbered: CIS Ubuntu 24.04
# Benchmark v2.0.0 has no number for the hard core limit. It is not 1.5.10,
# which belongs to systemd-coredump ProcessSizeMax.
* hard core 0
