---
# 1.3.1.4. Stops an unprivileged user creating a user namespace in which an
# unconfined profile would apply. Ubuntu 24.04 ships this at 1 already.
exorcism_apparmor_restrict_unprivileged_unconfined: 1
