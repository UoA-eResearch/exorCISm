---
# Which time daemon to configure: auto, timesyncd or chrony
exorcism_time_daemon: auto

# NTP sources. Empty leaves the daemon's own configuration alone
exorcism_ntp_servers: []

# 2.3.2.1. Fallback NTP sources, used only by systemd-timesyncd
exorcism_ntp_fallback_servers: []
